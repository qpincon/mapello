- When changing coutour width / contour color on the land layer, the changes are not applied, and I have "DOMException: Node.insertBefore: The new child is an ancestor of the parent" in the console
- The accordions are not collapsing when clicking on the header
- The accordions header arrow is black instead of white
- Clicking on "instructions" in the app header does not show the instructions modal
- When changing any parameter on the macro tab, the changes are not applied
- The tooltip default style is not applied
- There is a display bug apparently in the tooltip, but it might be related to the point above
- When clicking the example tooltip, the inline style editor is not opened as it should
- "Color using data" does not work at all
- "Show legend" does not work neither, I have this error in the console: "can't access property "append", container is null". But it might be related to the point above
- Dragging and dropping tabs does not work in the macro tab
- The zooming behavior is erratic, especially on non-stallite projections